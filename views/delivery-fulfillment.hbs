<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Delivery Fulfillment</title>
    <style>
        form
        {
            display: flex;
            flex-direction: column;
            gap: 10px;
        }
        button
        {
            margin-bottom: 20px;
        }
    </style>
</head>

<body>
    <header>
        <h1>Delivery Fulfillment</h1>
        <a href="/logout">Logout</a>
    </header>

    <main>
        <h2>Driver Name: {{ driverName }}</h2>
        <h2>Driver License Plate: {{ licensePlate2 }}</h2>
        {{#if orders}}
            {{#each orders}}
                {{#if this.assignedTo}} <!-- Only display assigned orders -->
                    <section>
                        <h2>Delivery Fulfillment for Order #{{ this._id }}</h2>
                        <p>Customer: {{ this.customerName }}</p>
                        <p>Delivery Address: {{ this.deliveryAddress }}</p>
                        <p>Status: {{ this.status }}</p>

                        <form method="POST" action="/complete/{{ this._id }}">
                            <label for="photo">Take a photo as proof of delivery:</label>
                            <input type="file" id="photo" name="photo" accept="image/*" required>
                            <button type="submit">Mark as Delivered</button>
                        </form>
                    </section>
                {{/if}}
            {{/each}}
        {{else}}
            <p>No orders available.</p>
        {{/if}}
    </main>

    <footer>
        <!-- Footer content goes here -->
    </footer>
</body>

</html>